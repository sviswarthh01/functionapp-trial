# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: functions-trialflex
metadata:
  template: functions-trialflex@1.0.0
services:
  processor:
    project: .
    language: python
    host: function
